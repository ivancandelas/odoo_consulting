<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="email_template_meeting_minute" model="mail.template">
            <field name="name">Acta de Reunión - Envío al cliente</field>
            <field name="model_id" ref="meeting_minutes_base.model_meeting_minute"/>
            <field name="subject">Acta {{ object.name }} - {{ object.title }}</field>
            <field name="email_from">{{ (object.user_id.email_formatted or user.email_formatted) }}</field>
            <field name="partner_to">{{ object.partner_id.id if object.partner_id else '' }}</field>
            <field name="description">Envía al cliente el acta de la reunión con el PDF adjunto.</field>
            <field name="report_template_ids" eval="[(4, ref('meeting_minutes_report.action_report_meeting_minute'))]"/>
            <field name="lang">{{ object.partner_id.lang or user.lang }}</field>
            <field name="auto_delete" eval="True"/>
            <field name="body_html" type="html">
                <div style="margin:0px;padding:0px;font-size:13px;">
                    <p>
                        Estimado/a <t t-out="object.partner_id.name or ''"/>,
                    </p>
                    <p>
                        Adjunto encontrará el acta de la reunión
                        <strong t-out="object.title or ''"/>
                        <t t-if="object.scheduled_date">
                            celebrada el
                            <t t-out="format_datetime(object.scheduled_date, dt_format='dd/MM/yyyy HH:mm') or ''"/>
                        </t>.
                    </p>
                    <p t-if="object.objective">
                        <strong>Objetivo:</strong>
                        <span t-out="object.objective or ''"/>
                    </p>
                    <p t-if="object.commitment_ids">
                        En el documento encontrará los compromisos acordados y sus responsables.
                    </p>
                    <p>
                        Quedamos atentos a cualquier comentario o aclaración.
                    </p>
                    <p>
                        Saludos cordiales,<br/>
                        <strong t-out="object.user_id.name or user.name"/>
                        <t t-if="object.company_id">
                            <br/>
                            <span t-out="object.company_id.name or ''"/>
                        </t>
                    </p>
                </div>
            </field>
        </record>

    </data>
</odoo>
